---
mode: 'agent'
description: 'Plan a new fullstack feature end-to-end with backend, frontend, tests, and CI tasks'
---

Analyse the codebase and produce a complete Technical Spec for the following feature:

${input:Feature description}

The spec must include:
1. API contract (endpoints, request/response shapes)
2. Database changes (tables, migrations, indexes)
3. Backend task checklist (Migration → Model → Service → Controller → Resource → Route)
4. Frontend task checklist (Schema → Hook → Page → Components)
5. Test checklist (feature tests, unit tests, E2E)
6. Any CI/CD changes needed
7. Risks and open questions

Do NOT write any code yet — produce the plan only.
